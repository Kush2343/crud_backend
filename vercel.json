{
    "version":2,
    "builds":[
        {
            "src":"../backend/server.json",
            "use":"@vercel/node"
        }
    ],
    "routes":[
        {
            "src":"/(.*)",
            "dest":"/"
        }
    ]
}